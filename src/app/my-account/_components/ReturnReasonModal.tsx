import Modal from '@/components/modal/Modal'
import React from 'react'

function ReturnReasonModal() {
  return (
    <Modal isOpen={} onClose={} className='w-[530px] p-8'>
      hi
    </Modal>
  )
}

export default ReturnReasonModal
