import Modal from '@/components/modal/Modal'
import React from 'react'

function ReturnPaymentModal() {
  return (
    <Modal isOpen={} onClose={} className='w-[530px] p-8'>
      hello
    </Modal>
  )
}

export default ReturnPaymentModal
